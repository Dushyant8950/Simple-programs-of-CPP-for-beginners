#include<iostream>
using namespace std;
int main(){
    char ch;
    cout<<"enter special characters :";
    cin>>ch;
    int ascii = (int)ch;
    cout<<ascii;
}


//SPECIAL CHARACTER : A TO Z, a TO z, 0 TO 9, `, ~, !, @, #, $, ^, *, (, ), _, +, [, ], {, }, \, |, :, ;, ', ", (,), ., <, >, ?, /,etc.